version: '3.8'

services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    image: nginx
    container_name: nginxapp
    ports:
      - "8080:80"
    stdin_open: true
    tty: true
    restart: always

